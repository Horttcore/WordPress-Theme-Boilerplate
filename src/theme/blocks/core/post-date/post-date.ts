import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/post-date": {}
		}
	},
	"styles": {
		"blocks": {
			"core/post-date": {}
		}
	}
};

export default config;
