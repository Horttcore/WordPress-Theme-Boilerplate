import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/post-template": {}
		}
	},
	"styles": {
		"blocks": {
			"core/post-template": {}
		}
	}
};

export default config;
