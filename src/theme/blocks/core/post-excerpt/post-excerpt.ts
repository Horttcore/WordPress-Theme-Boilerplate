import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/post-excerpt": {}
		}
	},
	"styles": {
		"blocks": {
			"core/post-excerpt": {}
		}
	}
};

export default config;
