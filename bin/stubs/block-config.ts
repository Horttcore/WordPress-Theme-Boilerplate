import type { ThemeJson } from '../../../theme.d.js';

const blockConfig: Partial<ThemeJson> = {
    settings: {
        blocks: {
            "namespace/block": {}
        }
    },
    styles: {
        blocks: {
            "namespace/block": {}
        }
    }
};

export default blockConfig;
