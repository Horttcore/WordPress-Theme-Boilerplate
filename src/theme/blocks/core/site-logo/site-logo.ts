import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/site-logo": {}
		}
	},
	"styles": {
		"blocks": {
			"core/site-logo": {}
		}
	}
};

export default config;
