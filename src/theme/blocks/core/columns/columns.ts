import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/columns": {}
		}
	},
	"styles": {
		"blocks": {
			"core/columns": {}
		}
	}
};

export default config;
