import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/navigation": {}
		}
	},
	"styles": {
		"blocks": {
			"core/navigation": {}
		}
	}
};

export default config;
