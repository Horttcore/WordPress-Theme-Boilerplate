import type { ThemeJson } from '../../../theme.d.js';

const config: Partial<ThemeJson> = {
	"settings": {
		"blocks": {
			"core/list-item": {}
		}
	},
	"styles": {
		"blocks": {
			"core/list-item": {}
		}
	}
};

export default config;
